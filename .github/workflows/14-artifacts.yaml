name: 14 - working with Artifacts


on:
    workflow_dispatch:



jobs:
    test-build:
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: 14-artifacts/my-react-app-14artifacts
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: Setup Node
              uses: actions/setup-node@v3
              with:
                node-version: 25.x
            - name: Download Cached dependencies
              uses: actions/cache@v3
              id: cache
              with:
                  path: 14-artifacts/my-react-app-14artifacts/node_modules
                  key: deps-node-modules-${{ hashFiles('14-artifacts/my-react-app-14artifacts/package-lock.json') }}            
            - name: Install dependencies
              if: steps.cache.outputs.cache-hit != 'true'
              run: npm ci    
            - name: Unit Tests
              run: npm run test
            - name: Build code
              run: npm run build
            - name: Upload Build
              uses: actions/upload-artifact@v3
              with:
                name: app
                path: 14-artifacts/my-react-app-14artifacts/build/
    deploy: 
        runs-on: ubuntu-latest
        steps:
          - name: Download Build Artifact
            uses: actions/download-artifact@v3
            with:
              name: app
              path: build
          - name: Show folder structure
            run: ls -R
        
        